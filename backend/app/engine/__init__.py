"""Adaptive AI engine.

Decision logic (skill controller, subject router, plan router, scoring) is plain
Python: reproducible, explainable, cheap and unit-tested. The LLM only scores
answers and phrases questions (AI_Engine_Spec §1).
"""

ENGINE_VERSION = "0.1.0"
